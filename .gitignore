bin/
obj/
out/
*.dll
*.pdb
*.user
.vs/
.idea/

# Local MCP server config (machine-specific paths)
.mcp.json

# Steam Workshop build output (regenerated by scripts/package-workshop.sh); the workspace
# config (workshop.json, image.png) stays tracked.
dist/workshop/content/

# Workshop item id — kept local only (private item; not committed to this public repo).
# Recreate with: echo <itemid> > dist/workshop/mod_id.txt
dist/workshop/mod_id.txt

# ModUploader CLI binary (downloaded per-machine)
tools/
